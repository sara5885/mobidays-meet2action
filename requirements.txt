duckdb>=1.0.0
pydantic>=2.6.0
polars>=1.0.0
pyarrow>=15.0.0
streamlit>=1.33.0
python-dotenv>=1.0.0
google-generativeai>=0.7.0
plotly>=5.20.0
pytest>=8.0.0

# (선택) 로컬 Whisper STT용 — 용량이 커서 STT 가산점 쓸 때만 설치
#   pip install openai-whisper && brew install ffmpeg
# openai-whisper>=20231117

# Ollama provider는 추가 패키지 불필요 (표준 라이브러리 urllib 사용)

# (선택) 한국어 형태소 분석기 — 키워드 추출 시 명사만 정확히 뽑음. 없으면 정규식 폴백.
# pip install kiwipiepy
# kiwipiepy>=0.17
